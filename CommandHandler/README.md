# App with a command handler

A playground for command handling

## Install all packages

`npm run restore` or `npm run restore:global`

## Development server

Run `npm start` for a dev server

## Build

## Running unit tests